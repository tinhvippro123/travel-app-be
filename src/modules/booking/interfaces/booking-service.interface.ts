import { Booking, CreateBookingDto, UpdateBookingDto } from '@modules/booking';import { IBaseService } from '@common/interfaces';export abstract class IBookingService extends IBaseService<  Booking,  CreateBookingDto,  UpdateBookingDto> {  abstract findByUserId(userId: string): Promise<Booking[]>;  abstract createForUser(    userId: string,    dto: CreateBookingDto,  ): Promise<Booking>;}